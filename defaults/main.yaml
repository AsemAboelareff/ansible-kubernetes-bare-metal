kubernetes:
  config: "/home/{{ ansible_user }}/.kube"
  CIDR: "10.0.1.0/16"

calico:
  version: "v3.26.4"

metallb:
  addresses: 10.0.1.4/32 # 192.168.1.50
  version: "v0.13.12"

ingress_nginx:
  version: "v1.9.4"
  provider: "cloud"

cert_manager:
  version: "v1.13.3"

letsencrypt:
  email: asemasem1@gmail.com

nfs:
  whitelist_ips: 10.0.1.0/27 # Ips allow access to NFS server 192.168.1.0 -> 192.168.1.31